'use strict';

import {ECMFile} from "./storageService";
import {StartDocumentAnalysisResponse} from "aws-sdk/clients/textract";
    export async function start(eFile: ECMFile): Promise<StartDocumentAnalysisResponse> {
       return await start(eFile);

}
